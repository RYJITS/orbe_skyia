export const createCheckoutSession = async (_uid: string, _priceId: string): Promise<void> => {
    throw new Error("Paiement Stripe non configure sur Hostinger pour cette version. Les credits locaux et codes promo restent disponibles.");
};
